import styled from 'styled-components/native'

import { CustomView } from 'common/CustomView'

export const StyledCryptoDataScreen = styled(CustomView)`
  background-color: ${({ theme }) => theme.colors.primary};
`
