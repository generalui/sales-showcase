import React from 'react'

import { Text } from 'widgets/Text'

import { Styled{{pascalCase name}} } from './styled'
import { Props } from './types'

export const {{pascalCase name}} = (props: Props) => (
  <Styled{{pascalCase name}}>
    <Text>Hello there</Text>
  </Styled{{pascalCase name}}>
)
